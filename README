
; example how to create a migration file (up/down files).
lein migratus create add-users-table

; migrate
lein migratus migrate

; rollback
lein migrate rollback

; Run ‘down’ for all migrations that have been  run, and ‘up’ for all migrations.
lein migratus reset
